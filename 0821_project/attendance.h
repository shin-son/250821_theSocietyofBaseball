#pragma once
#include <iostream>
#include <fstream>
#include <string>
#include <vector>
#include <map>
#include <algorithm>

extern void load();
extern void compute();
extern void updateAttendance(std::string name);
extern void updateScore(std::string name, std::string day);